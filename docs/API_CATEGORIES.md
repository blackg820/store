# Categories API

Endpoints:
- `GET /categories`
- `POST /categories`
- `GET /categories/{category}`
- `PUT|PATCH /categories/{category}`
- `DELETE /categories/{category}`

Categories are exposed for dashboard product forms and catalog management. Flutter should prefer server-returned category payloads over hardcoded lists.
